# Saved timeline projects

This folder holds project files (`<name>.json`) written by the timeline drawer's Save button via `/api/projects`. Each file captures control values + keyframes (not the video).
